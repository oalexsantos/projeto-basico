import "./Footer.css"
import React from "react";

class Footer extends React.Component {
    render() {
        return (
            <footer>Desenvolvido por React</footer>
        );
    }
}

export default Footer;